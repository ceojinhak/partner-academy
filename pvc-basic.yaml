kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pa-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: pa-storageclass
