kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: basic-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: pa-storageclass
